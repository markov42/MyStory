//
//  ViewController.swift
//  MyStory
//
//  Created by Jing Liu on 2/25/23.
//

import UIKit

class ViewController: UIViewController {


    override func viewDidLoad() {
        super.viewDidLoad()
        /*
         // Create an outlet for image view
         let imageView = UIImageView(image: UIImage(named: "Image"))
         imageView.translatesAutoresizingMaskIntoConstraints = false
         imageView.contentMode = .scaleAspectFit
         
         // Set the background color of the image view's layer to orange
         imageView.layer.backgroundColor = UIColor.orange.cgColor
         
         // Set the corner radius of the image view to make it round
         imageView.layer.cornerRadius = imageView.frame.size.width / 2
         imageView.clipsToBounds = true
         
         // Add the image view to controller's view
         view.addSubview(imageView)
         
         // Add autolayout constraints to position and size the image view
         NSLayoutConstraint.activate([
         imageView.centerXAnchor.constraint(equalTo: view.centerXAnchor),
         imageView.topAnchor.constraint(equalTo: view.topAnchor, constant: view.bounds.size.height / 3),
         imageView.widthAnchor.constraint(equalToConstant: 250),
         imageView.heightAnchor.constraint(equalToConstant: 250)
         ])
         }
         */
    }
}



